{
  "name": "firebase",
  "version": "1.0.0",
  "description": "firbase",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log --copy-files",
    "start": "nodemon run build && nodemon dist/index.js"
  },
  "author": "tomnyson",
  "license": "ISC",
  "dependencies": {
    "cron": "^1.4.1",
    "firebase-admin": "^6.0.0",
    "http": "0.0.0",
    "moment": "^2.22.2",
    "mysql": "^2.16.0",
    "mysql-events": "0.0.11",
    "zongji": "^0.4.6"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "grunt": "^1.0.3",
    "grunt-contrib-watch": "^1.1.0",
    "grunt-execute": "^0.2.2",
    "rimraf": "^2.6.2"
  }
}
